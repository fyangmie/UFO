services:
  - type: web
    name: ufo-local-deployment
    runtime: python
    buildCommand: python -m pip install -r requirements.txt
    startCommand: bash scripts/start_cloud.sh
    envVars:
      - key: UFO_BACKEND_URL
        value: http://127.0.0.1:8000
